<%= simple_form_for(@email_list) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
	<%= f.association :project %>
    <%= f.input :name %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
